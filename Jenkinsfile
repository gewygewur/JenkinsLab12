pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo "Version A - local change"
                echo "Version B - remote change"
            }
        }
    }
}
